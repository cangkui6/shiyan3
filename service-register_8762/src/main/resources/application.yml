server:
  port: 8762

eureka:
  instance:
    hostname: register2  # Eureka 服务实例名
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/  # 互相注册
  server:
    enable-self-preservation: false
